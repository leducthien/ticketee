<li>
  <% if can?(:tag, @ticket.project) || current_user.admin? %>
    <%= link_to "x",
      remove_ticket_tag_path(@ticket, tag),
      :method => :delete,
      :remote => true,
      :id => "delete-#{tag.name.parameterize}" %>
  <% end %>
  <%= link_to tag.name, ticket_tag_path(@ticket, tag) %>
</li>
